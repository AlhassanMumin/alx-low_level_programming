#include <string.h>
#include "main.h"
/**
 * strcmp - The func compares two strings
 *
 * @s1: The value to be compared to
 * @s2: The value to compare to
 * Return: 1 if  s1 is higheter than s2
 * 	   0 if s1 and s2 are same
 * 	   -1 if s2 is higher than s1
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
