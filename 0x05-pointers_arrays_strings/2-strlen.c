#include <string.h>
/**
 * _strlen - It will return the length of the string
 *
 * @s: The pointer to the string variable
 * Return: length of the string to be returned
 */
int _strlen(char *s)
{
	return (strlen(s));
}

