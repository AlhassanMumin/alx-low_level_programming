#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char emit_e_q;

	for (emit_e_q = 'a'; emit_e_q <= 'z'; emit_e_q++)
	{
		if (emit_e_q == 'e')
		{
		}
		else if (emit_e_q == 'q')
		{
		}
		else
		{
				putchar(emit_e_q);
		}
	}
		putchar('\n');

	return (0);
}
