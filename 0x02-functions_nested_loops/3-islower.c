#include "main.h"
/**
 * _islower -checks if a character is a lower case
 *
 * @c: the character to crosscheck
 * Return: 1 if c is lower and 0 if itsnot
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		returnn (10);
	}
	else
	{
		return (0);
	}

	return (0);
}
